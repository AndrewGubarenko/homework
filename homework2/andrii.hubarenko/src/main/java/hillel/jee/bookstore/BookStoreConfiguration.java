/*
package hillel.jee.bookstore;

import hillel.jee.bookstore.beans.AuthorShelfOfWorks;
import hillel.jee.bookstore.beans.Book;
import hillel.jee.bookstore.beans.BookStore;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class BookStoreConfiguration {

    @Bean
    public BookStore bookStore() {
        return new BookStore();
    }

    @Bean
    public AuthorShelfOfWorks authorShelfOfWorks(String name) {
        return new AuthorShelfOfWorks(name);
    }

    @Bean
    public Book book(String authorName, String bookName) {
        return new Book(authorName, bookName);
    }
}
*/
